{
  "name": "ares-frontend-test-04",
  "version": "1.0.0",
  "description": "Auto-generated wrapper for deployment",
  "scripts": {
    "start": "cd frontend && npm start",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}